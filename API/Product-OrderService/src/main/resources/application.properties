spring.application.name=Product-OrderService

# LOCAL (VS Code / java.exe): usa localhost
spring.datasource.url=jdbc:postgresql://localhost:5432/${DB_NAME:marafloresdb}
spring.datasource.username=${DB_USERNAME:postgres}
spring.datasource.password=${DB_PASSWORD:german}

spring.jpa.hibernate.ddl-auto=update
# Puedes omitir el dialecto (Hibernate lo detecta):
# spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

server.port=8082
springdoc.swagger-ui.path=/swagger-ui.html
